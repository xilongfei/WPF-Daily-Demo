<Window
    Height="450"
    Title="MainWindow"
    Width="800"
    mc:Ignorable="d"
    x:Class="WpfApp3.Views.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:globalization="clr-namespace:System.Globalization;assembly=System.Runtime"
    xmlns:languages="clr-namespace:WpfApp3.Languages"
    xmlns:local="clr-namespace:WpfApp3.Views"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:sys="clr-namespace:System;assembly=mscorlib"
    xmlns:viewModels="clr-namespace:WpfApp3.ViewModels"
    xmlns:wpfApp3="clr-namespace:WpfApp3"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <StackPanel>
            <TextBlock Text="{I18N {x:Static languages:LangKeys.Name}}" />

            <ComboBox ItemsSource="{Binding Items}" SelectedItem="{Binding SelectedItem}">
                <ComboBox.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{I18N {Binding}}" />
                    </DataTemplate>
                </ComboBox.ItemTemplate>
            </ComboBox>

            <TextBlock Text="{I18N {x:Static languages:LangKeys.DateLabel}}" />

            <TextBlock Text="{Binding CurrentTime}" />

        </StackPanel>
    </Grid>
</Window>
